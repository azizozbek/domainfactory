AuthType Basic
AuthName "Passwortgeschütztes Verzeichnis"
AuthUserFile /httpdocs/public/.htpasswd
Require valid-user


<IfModule mod_rewrite.c>
    RewriteEngine On
    RewriteBase /
    RewriteCond %{REQUEST_FILENAME} !-f
    RewriteCond %{REQUEST_FILENAME} !-d
    RewriteRule ^ index.php [QSA,L]
</IfModule>